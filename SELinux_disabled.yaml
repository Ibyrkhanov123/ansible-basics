---
  - name: Changing the configuration for SELinux file and Reboot system
    hosts: localhost
    tasks:
      - name: Disable SELinux
        lineinfile:
          path: /etc/selinux/config
          regexp: '^SELINUX='
          line: SELINUX=disabled
      - name: Reboot the
        command: reboot